server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: yzzy.top
      port: 8500
      discovery:
        service-name: ${spring.application.name}


